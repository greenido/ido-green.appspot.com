name: push to GAE
on: [push]
jobs: 
    steps:
    - name: upload to app engine
      uses: actions/greenido/gcloud@master
      env: app deploy --project ido-green
    - run: cd ./ido-green && gcloud app deploy --project ido-green
